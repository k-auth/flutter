name: k_auth
description: 한국형 소셜 로그인 Flutter 라이브러리. 카카오/네이버/구글/애플 OAuth를 Flutter에서 쉽게 구현.
version: 0.1.0
homepage: https://github.com/k-auth/flutter
repository: https://github.com/k-auth/flutter
issue_tracker: https://github.com/k-auth/flutter/issues

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  kakao_flutter_sdk: ^1.9.0
  flutter_naver_login: ^1.8.0
  google_sign_in: ^6.1.0
  sign_in_with_apple: ^5.0.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
